apiVersion: v1
kind: Service
metadata:
  labels:
    app: javaapp1
  name: javaapp1
spec:
  type: NodePort
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP
  selector:
    app: javaapp1
